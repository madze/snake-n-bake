/**
* This mode is all about chasing tail (our tail to be specific, though really any tail would do in theory)
*/

module.exports = (snake, gameState) => {
  return () => {
    console.log('MODE - Chase Tail! (our tail)')

  }
}